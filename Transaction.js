class Transaction {
    constructor(senderId, receiverId, value) {
        this.senderId = senderId;
        this.receiverId = receiverId;
        this.value = value;
    }
}

export default Transaction;
